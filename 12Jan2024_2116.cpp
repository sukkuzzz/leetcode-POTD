#include <bits/stdc++.h>
#include <iostream>

using namespace std;

typedef long long ll;
const int M = 1e9 + 7;
void solve()
{
    string s, locked;
    cin >> s >> locked;
    cout << canBeValid(s, locked) << endl;
}
bool canBeValid(string s, string locked)
{
}
int main()
{
    int t = 1;
    while (t--)
    {
        solve();
    }
    return 0;
}